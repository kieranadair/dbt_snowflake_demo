version: 2

sources:
  - name: demo_schema
    database: demo
    schema: demo_schema
    description: data sources related to the snowflake & dbt course
    tables:
      - name: weather_silver
      - name: bikedata_raw